# Classifier Flask service — runtime dependencies.
#
# Direct imports in app.py: flask, pandas, numpy, joblib (os is stdlib).
# scikit-learn, xgboost and lightgbm are NOT imported by app.py but ARE
# required at import time to unpickle models/*.pkl.
#
# Version provenance (see docs/ML_MODEL.md):
#   - scikit-learn==1.7.0 is the version recorded INSIDE scaler.pkl and the
#     model label encoders; pinning it removes InconsistentVersionWarning.
#   - xgboost and lightgbm training versions are UNKNOWN. The pins below are
#     the versions verified locally to load the artifacts warning-free and
#     reproduce the reference prediction.

Flask==3.1.3
pandas==2.3.3
numpy==2.3.4
joblib==1.5.3
scikit-learn==1.7.0
xgboost==3.0.5
lightgbm==4.7.0

# Production WSGI server (not imported by app.py; used by the start command).
gunicorn==23.0.0
